do {
  const array = [`Ruhul`, `Amin`, `Rakib`];
  const person = array.map((personNames) => personNames);
  const randomValue = Math.floor(Math.random() * 10 + 1);
} while (randomValue === array.length); {
console.log(person)
}