const array = [10, 1, 3, 5, 6];
//* array insert
// array[0] = 9
// array.push(10)
// array.unshift(0)
// array.splice(2,0,100)

//* array Delete
// array[0] = undefined;
// array.pop()
// array.shift()
// array.splice(3,1)
//* arrray remove and update
array.splice(3, 1, 10);

console.log(array);
